import { useState } from 'react'
import { X } from 'lucide-react'

export function CreateTaskModal({ close, onSubmit }) { const [form, setForm] = useState({ title: '', description: '', status: 'TODO', priority: 'MEDIUM', dueDate: '' }); const [saving, setSaving] = useState(false); const [error, setError] = useState(''); const update = event => setForm({ ...form, [event.target.name]: event.target.value }); const submit = async event => { event.preventDefault(); if (!form.title.trim()) return setError('Title is required'); setSaving(true); try { await onSubmit({ ...form, title: form.title.trim(), dueDate: form.dueDate || undefined }) } catch (err) { setError(err.message) } finally { setSaving(false) } }; return <div className="modal-layer"><form className="modal requirement-modal" onSubmit={submit}><button type="button" className="modal-close" onClick={close}><X size={20} /></button><h2>Create New Task</h2><label>Title <span>*</span><input name="title" value={form.title} onChange={update} placeholder="e.g., Q3 Marketing Report" /></label><label>Description<textarea name="description" value={form.description} onChange={update} placeholder="Add detailed context..." rows="4" /></label><div className="modal-grid"><label>Status<select name="status" value={form.status} onChange={update}><option value="TODO">To Do</option><option value="IN_PROGRESS">In Progress</option><option value="DONE">Done</option></select></label><label>Due Date<input type="date" name="dueDate" value={form.dueDate} onChange={update} /></label></div><label>Priority<select name="priority" value={form.priority} onChange={update}><option value="LOW">Low</option><option value="MEDIUM">Medium</option><option value="HIGH">High</option></select></label>{error && <p className="red-text">{error}</p>}<div className="modal-actions"><button type="button" className="cancel" onClick={close}>Cancel</button><button type="submit" className="create-button" disabled={saving}>{saving ? 'Saving...' : 'Save Task'}</button></div></form></div> }
